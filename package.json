{
  "scripts": {
    "test": "jest",
    "test:unit": "jest tests",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "axios-mock-adapter": "^1.22.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "axios": "^1.16.1"
  }
}
